/*

To find the square root of a number, use the sqrt() function:

*/

#include<stdio.h>
#include<math.h>

int main()
{
    int num,num1;

    num=sqrt(4);
    num1=sqrt(16);
    printf("%d", num);
    printf("\n%d ",num1);

    return 0;
}
